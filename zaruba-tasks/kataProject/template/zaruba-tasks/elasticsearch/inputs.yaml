inputs:

  runElasticsearchInLocal:
    default: yes
    options:
      - yes
      - no
    allowCustom: false
    description: Whether to run elasticsearch locally
    prompt: Run elasticsearch locally?