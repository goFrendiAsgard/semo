tasks:

  makeHelmDeploymentGenerator:
    icon: 🪄
    extend: zrbMake
    inputs:
      - appDirectory
      - chartName
    configRef: makeHelmDeploymentGenerator
    envRef: makeHelmDeploymentGenerator